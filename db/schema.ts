import {sqliteTable,text,real,integer,index,primaryKey} from "drizzle-orm/sqlite-core";
export const entries=sqliteTable("nutrition_entries",{id:text("id").primaryKey(),userId:text("user_id").notNull(),date:text("date").notNull(),meal:text("meal").notNull(),name:text("name").notNull(),grams:real("grams").notNull(),calories:real("calories").notNull(),protein:real("protein").notNull(),carbs:real("carbs").notNull(),fat:real("fat").notNull(),source:text("source").notNull().default(""),createdAt:integer("created_at").notNull()},t=>[index("idx_entries_user_date").on(t.userId,t.date)]);
export const water=sqliteTable("water_entries",{id:text("id").primaryKey(),userId:text("user_id").notNull(),date:text("date").notNull(),amount:integer("amount").notNull(),createdAt:integer("created_at").notNull()},t=>[index("idx_water_user_date").on(t.userId,t.date)]);
export const goals=sqliteTable("nutrition_goals",{userId:text("user_id").notNull(),date:text("date").notNull(),calories:real("calories").notNull(),protein:real("protein").notNull(),carbs:real("carbs").notNull(),fat:real("fat").notNull(),water:integer("water").notNull(),mode:text("mode").notNull()},t=>[primaryKey({columns:[t.userId,t.date]})]);
